# gitops-demo environment configuration
# Copy to .env and fill in required values:
#   cp .env.example .env

# ---- Application ----
PORT=8080

# ---- Build ----
APP_NAME=gitops-demo
# VERSION, COMMIT, and BUILD_TIME are derived from git automatically.
# Uncomment to override:
# VERSION=dev
# COMMIT=unknown
# BUILD_TIME=unknown

# ---- GitHub / FluxCD (required for k3d-setup) ----
GITHUB_TOKEN=
GITHUB_USER=

# ---- Kubernetes / k3d ----
CLUSTER_NAME=gitops-demo
REPO_NAME=gitops-demo
BRANCH=main
